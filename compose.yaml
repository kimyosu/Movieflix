services:
  postgres:
    container_name: db
    image: 'postgres:latest'
    env_file:
      - .env
    ports:
      - "5432:5432"
    networks:
      - my-network
networks:
  my-network:
    driver: bridge